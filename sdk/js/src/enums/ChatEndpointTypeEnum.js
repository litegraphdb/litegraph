/**
 * Enum for chat endpoint types.
 *
 * @readonly
 * @enum {string}
 */
export const ChatEndpointTypeEnum = Object.freeze({
  /** Embedding endpoint. */
  Embedding: 'Embedding',

  /** Completion endpoint. */
  Completion: 'Completion',
});
